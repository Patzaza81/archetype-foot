name: Temporary decision core validation
on:
  push:
    branches: ["moteur-decision-v1"]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - run: python -m pip install pytest
      - run: PYTHONPATH=. pytest -q tests/test_moteur_decision_core.py tests/test_moteur_decision_risk.py tests/test_moteur_decision_justification.py
